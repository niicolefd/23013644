#include <stdio.h>
#include <math.h>
#define Pi 3.14

int main() {
    float raio, area;
    double quadrado;
    
    printf("Digite o raio do circulo: ");
    scanf("%f", &raio);
    
    area = Pi * pow(raio, 2);
    
    printf("A area do circulo e: %.2f", area);
    
return 0;
}
