#include <stdio.h>
#include <stdlib.h>

int main() {
    float raio, area;
    
    printf("Digite o raio: ");
    scanf("%f", &raio);
    
    area = 3.14 * raio * raio;
    printf("Area total do circulo: %g \n", area);

return 0;
}
