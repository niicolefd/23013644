#include <stdio.h>

int main() {
    int palpite;
    
    do {
        printf("Adivinhe o numero entre 1 e 10: ");
        scanf("%d", &palpite);
    } 
    
    while (palpite != 7);

    printf("Parabéns! Você acertou o número 7.\n");

return 0;
}
