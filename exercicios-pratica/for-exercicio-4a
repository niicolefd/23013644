#include <stdio.h>

int main() {
    int soma = 0;
    
    for (int i = 1; i <= 500; i++) {
        if (i % 2 == 0) {
            soma += i;
        }
    }
    
    printf("A soma dos números pares de 1 até 500 é: %i\n", soma);

    return 0;
}
