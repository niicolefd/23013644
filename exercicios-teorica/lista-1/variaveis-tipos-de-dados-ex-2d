#include <stdio.h>
#include <math.h>
#define Pi 3.14159

int main() {
    float raio, area;
    
    printf("Digite o raio do circulo: ");
    scanf("%f", &raio);
    
    area = Pi * raio * raio;
    printf("A area do circulo e: %.2f", area);
    
    return 0;
}
